fastapi==0.111.0
uvicorn[standard]==0.29.0
networkx==3.3
pydantic>=2.0.0
python-dotenv==1.0.1
httpx==0.27.0

# LLM providers — install the one matching your LLM_PROVIDER setting
anthropic>=0.28.0        # LLM_PROVIDER=anthropic (default)
openai>=1.0.0            # LLM_PROVIDER=openai
